export class UserObject {

    constructor(username, email) {
        this.username = username,
            this.email = email
    }

    userob() {
        console.log('hoooolaaa', this.username, this.email)
    }
}