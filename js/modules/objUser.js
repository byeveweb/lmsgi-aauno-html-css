export class User {

    constructor(username, email, password, passwordconfirm, checkpolicity) {

        this.username = username,
            this.email = email,
            this.password = password,
            this.passwordconfirm = passwordconfirm,
            this.checkpolicity = checkpolicity

    }

    setUserObject() {
        console.log('dddddddd')
    }


}